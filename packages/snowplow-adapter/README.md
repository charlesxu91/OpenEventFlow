# @openeventflow/snowplow-adapter

Adapter that converts normalized OpenEventFlow events into Snowplow self-describing event payloads.

It accepts a Snowplow tracker-like object with `trackSelfDescribingEvent` or `track`.
