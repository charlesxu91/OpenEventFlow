# @openeventflow/core

Shared JavaScript analytics runtime used by Web, React, and React Native packages.

It provides:

- event normalization
- identity context
- session context
- consent checks
- PII filtering
- memory queue
- batch flushing through an injected transport
